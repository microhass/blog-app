<div class="user-list">
  <% if @users.count > 0 then %>
    <%= render @users %>
  <% else %>
    <p class='no-content'>No users created</p>
  <% end %>

  <%= link_to 'New User', '/users/new', class: 'btn' %>
</div>
